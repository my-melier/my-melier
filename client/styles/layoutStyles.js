import { StyleSheet } from 'react-native';

const layoutStyles = StyleSheet.create({
  container: {
    // flex: 1,
    margin: 20,
  },
  subContainer: {
    marginTop: 10,
    marginBottom: 10,
  },
  padding: {
    paddingTop: 10,
    paddingBottom: 10,
  },
});

export default layoutStyles;
